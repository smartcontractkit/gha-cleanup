{
  "name": "gha-selfhosted-cleanup",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "build:watch": "ncc build src/index.ts -w -o dist",
    "start": "node ."
  },
  "devDependencies": {
    "@types/node": "^14.11.2",
    "@vercel/ncc": "^0.25.1",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/github": "^4.0.0",
    "@octokit/plugin-throttling": "^3.3.1"
  }
}
